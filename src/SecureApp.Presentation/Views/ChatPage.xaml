<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="SecureApp.Presentation.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <!-- Content is set in code-behind (ChatThreadView needs a constructor-injected
         ISharedLibraryService, which a XAML-declared child element can't receive — only
         ChatPage's own DI-resolved constructor can). See ChatThreadView's own remarks. -->

</ContentPage>
