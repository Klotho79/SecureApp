<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="SecureApp.Presentation.Views.GroupChatPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <!-- Content is set in code-behind (GroupChatThreadView needs a constructor-injected
         ISharedLibraryService, which a XAML-declared child can't receive). The same view is also
         hosted persistently in ChatListPage's phone overlay — see GroupChatThreadView's own remarks. -->

</ContentPage>
